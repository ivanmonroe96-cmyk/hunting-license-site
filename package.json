{
  "name": "hunting-license-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:css": "postcss assets/css/styles.css -o assets/css/styles.build.css",
    "watch:css": "postcss assets/css/styles.css -o assets/css/styles.build.css --watch",
    "build": "npm run build:css && hugo --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.24",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.5.0"
  }
}
